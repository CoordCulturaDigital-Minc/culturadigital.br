Marcelo Mesquita
http://www.marcelomesquita.com

Painter Theme

1. Installation
Upload the theme to your '/wp-content/themes/' directory. Them go to 'design > themes' and activate the theme.

2. Configuration

2.1. Custom Theme
Extra functionalities configurable. Here the user can check the post informations that he wanna let visible for the 
visitors.

2.3. Custom Header
To upload a custom image to the header, go to 'design > custom header' chose a image from your computer and upload the 
file. If the size of the image is diferent of the acepted values, crop the image to the correct size.

2.3. Custom Colors
To change the colors of the theme, go to 'design > custom colors' and select one by one the color of the itens of the 
theme. You can load some pre-defined color schemes from the select area, after load the color scheme it's possible to 
change any itens.

2.4. Highlights
To use Highlight posts, create a category with 'highlight' for slug name. Every post marked for this category will be 
showed on the slide of your home page. The highlight load the first image from the post galery and it's cofigured for 
the default WordPress thumbnail size (150px x 150px).

3. Changelog
version 0.6:
 - First live version
 - Widget (defined in 'Design > Widgets')
 - Custom Header (defined in 'Design > Custom Header')
 - Custom Colors (defined in 'Design > Custom Colors')
 - Highlights using lead_manager plugin
 - BreadCrumb using breadcrumb_xt plugin
 - Captcha on comments using authimage plugin

version 0.7:
 - CSS corrections

version 0.8:
 - Highlight by categories instead 'Lead Manager' plugin

version 0.9:
 - CSS corrections for IE 6
 - Alternative avatar function for WordPress < 2.3

version 1.0:
 - Custom Theme (defined in 'Design > Custom theme')

version 1.1:
 - Alternative Custom Colors function for WordPress < 2.6
 - Threaded Comments
 - Custom Theme working on single and page templates
 - Custom date format (defined in 'options > general')
 - Posts navigation (defined in 'design > Custom theme')
 - BreadCrumb using yoast_breadcrumbs plugin

4. ToDo
 - Fix submenu itens on Opera, Safari
 - Custom header size
 - Custom column size and align
 - Custom fonts



Enjoy...
